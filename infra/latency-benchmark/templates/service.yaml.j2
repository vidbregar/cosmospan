apiVersion: v1
kind: Service
metadata:
  name: ping-svc-{{ node_ip_clean }}
  namespace: {{ benchmark_namespace }}
spec:
  selector:
    app: ping-pod-{{ node_ip_clean }}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
